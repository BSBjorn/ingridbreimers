(function( root, $, undefined ) {
	"use strict";

	$(function () {
		// DOM ready, take it away

			$(".unSlider").unslider({
				nav: true,
				autoplay: true,
				infinite: true
			});
	});

} ( this, jQuery ));
