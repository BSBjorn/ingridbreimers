(function( root, $, undefined ) {
	"use strict";

	$(function () {
		// DOM ready, take it away

			$(".unSlider").unslider({
				nav: false,
				autoplay: true,
				infinite: true
			});
	});

} ( this, jQuery ));
